# 项目交接文档 — HomewardBird 博客

## 一、项目基本信息

| 项目 | 详情 |
|------|------|
| **仓库地址** | `https://github.com/HomewardBird/First_single_blog` |
| **当前分支** | `release-ver` |
| **本地路径** | `F:\First_single_blog` |
| **框架** | Quartz v5.0.0（基于 Preact + TypeScript 的静态博客） |
| **构建命令** | `npx quartz build`（输出到 `public/`） |
| **插件安装** | `npm run install-plugins`（从 GitHub 克隆 quartz-community 插件到 `.quartz/plugins/`） |
| **Cloudflare Pages** | 已绑定，构建命令待修正 |

---

## 二、当前文件清单（本地 & GitHub 同步）

### content/（所有文章完整）

```
content/index.md
content/个人博客.md
content/关于.md
content/杂谈/01 网名的故事.md
content/杂谈/index.md
content/留言.md
content/计算机入门指南/index.md
content/计算机入门指南/基础入门/01 认识你的电脑.md    (~11KB，万字文章)
content/计算机入门指南/基础入门/index.md
content/随笔/01.md
content/随笔/index.md
content/images/shu.jpg                                   (9.5MB)
content/images/yingjianzuoye.png                         (1.6MB)
```

### 用户修改过的源码文件（已推送）

- `quartz/components/renderPage.tsx` — 加载界面、汉堡菜单、音乐播放器
- `quartz/static/custom.js` — 汉堡菜单交互、背景色切换、字体设置、音乐控制
- `quartz/styles/custom.scss` — 背景视频/图片、汉堡菜单样式、音乐按钮样式
- `.quartz/plugins/comments/src/components/Comments.tsx` — 评论组件修改
- `.quartz/plugins/comments/src/components/scripts/comments.inline.ts` — 评论脚本
- `.quartz/plugins/explorer/src/components/styles/explorer.scss` — 移动端菜单毛玻璃效果
- `quartz.config.yaml` — 网站标题（安巢鸟的网站）、主题颜色、插件配置

### 不在仓库中的文件（.gitignore 排除）

```
node_modules/           （npm 依赖，太大）
public/                 （构建产物）
static/                 （音乐文件 .m4a/.mp3，太大）
quartz/static/          （背景视频 .mp4 + 音乐，太大）
dark_bg.mp4             （背景视频）
light_bg.mp4            （背景视频）
docs/                   （Quartz 官方文档）
content/.obsidian/      （Obsidian 编辑器配置）
```

---

## 三、Git 提交历史（release-ver 分支，从最新到最旧）

| 提交 | 说明 |
|------|------|
| `a822353` | 添加图片资源 |
| `972c896` | 应用UI改进：汉堡菜单、音乐控制、页面优化 |
| `2fc5e41` | 源码+内容初始化（根提交，这是 push 的起点） |

**重要说明：** `2fc5e41` 是孤儿提交（无父提交），之前的全部历史已被丢弃。
之前的历史如果还要恢复，可以在 dangling commits 中找到：

- `49b524a` — 包含所有文件（含大文件）的完整提交，有 12 个修改文件
- `1dd872a` — 同样包含完整文件，可用 `git checkout 1dd872a -- <文件>` 恢复单个文件
- `7c46a79` — stash 索引提交
- `17ec4df` — 远程旧分支的 HEAD（site update）

---

## 四、stash 状态

```
stash@{0}: WIP on dev: 17ec4df site update
  - 包含 content/index.md, renderPage.tsx, custom.js, custom.scss 等 UI 修改
  - 已应用但保留备份

stash@{1}: WIP on dev: 256b4f3 添加评论违禁词自动审核系统
  - 包含 content/关于, 杂谈/index, 留言, 01认识你的电脑.md 的内容修改
  - 已应用且被 dropped
```

---

## 五、Cloudflare 部署问题（尚未解决）

### 错误信息

最新部署日志显示卡在 `Quartz v5.0.0`，然后超时。原因分析：

1. CF 环境是全新克隆，没有 `.quartz/plugins/` 的完整依赖
2. `install-plugins` 脚本没有运行，插件缺少构建产物（`dist/`）和依赖
3. **第一次构建**报错 `Could not resolve "../quartz"` — 已通过补上 `quartz.ts` 修复
4. **第二次构建**报错 `Could not resolve "../../.quartz/plugins"` — 已通过补上 `.quartz/` 目录修复
5. **当前日志**卡在 `Quartz v5.0.0` — 推测是插件依赖未安装导致

### 需要修改的 Cloudflare 配置

**位置：** Settings → Build & deploy → Build configuration

**构建命令改为：**
```
git fetch --unshallow && npm run install-plugins && npx quartz build
```

**输出目录：** `public`

**环境变量：** 如有问题添加 `NPM_CONFIG_REGISTRY=https://registry.npmjs.org/`

### 为什么需要 install-plugins

`quartz.config.yaml` 声明了 30+ 个社区插件，来自 `github:quartz-community/*`。
`install-plugins` 脚本会：

1. 从 GitHub 克隆每个插件到 `.quartz/plugins/<name>/`
2. 运行 `npm install --ignore-scripts` 安装插件依赖
3. 运行 `tsup` 构建插件，生成 `dist/` 目录
4. 为每个插件创建 `node_modules/preact` 等符号链接，指向根目录 `node_modules/`

原始命令 `npx quartz build` **不会**自动执行这些步骤。

### 本地 npm 镜像源问题

用户的 npm 配置了淘宝镜像 `https://cdn.npmmirror.com/`，该镜像**缺少** `@quartz-community/types` 包。
今天执行了以下命令切换为官方源：

```bash
npm config set registry https://registry.npmjs.org/
```

如果恢复对话时 npm install 报错，先检查注册表：

```bash
npm config get registry
```

---

## 六、git 恢复命令速查

```bash
# 恢复所有文章内容
git checkout 49b524a -- content/

# 恢复插件目录
git checkout 1dd872a -- .quartz/

# 恢复配置文件
git checkout 1dd872a -- quartz.ts Dockerfile .gitattributes .node-version .npmrc

# 恢复图片
git checkout 1dd872a -- content/images/yingjianzuoye.png
git checkout 8689315 -- content/images/shu.jpg

# 恢复 UI 修改（如果不小心丢了）
git stash pop stash@{0}

# 查看 stash 内容（不改动文件）
git stash show -p stash@{0}
```

---

## 七、本地运行和测试

```bash
# 构建并启动本地服务器
npx quartz build --serve --port 3000

# 仅构建（不启动服务器）
npx quartz build

# 安装插件依赖
npm run install-plugins

# 推送到 GitHub（可能需要 VPN/代理）
git push origin release-ver --force
```

---

## 八、对话中的决策记录

1. **选了强制推送方案** — 用本地覆盖远程，因为本地是最新版
2. **添加了 .gitignore** — 排除了 `content/.obsidian/`、`nul`
3. **选了全新分支推送** — 旧历史 179MB 太大传不上去，用孤儿提交重新开始
4. **排除了大文件** — `node_modules/`、`public/`、`static/`、媒体文件不提交到 git
5. **恢复了多个遗漏文件** — 每次发现缺失都从 git 历史中找回
6. **npm 镜像问题** — 淘宝镜像缺少 `@quartz-community/types`，改为官方源

---

## 九、给新对话的开场白

复制以下文字发给新对话的开头：

```
请帮我继续处理我的 Quartz 博客项目，路径是 F:\First_single_blog。

当前问题：
1. Cloudflare Pages 部署卡在 "Quartz v5.0.0"，需要修改构建命令为：
   git fetch --unshallow && npm run install-plugins && npx quartz build
   输出目录保持 public。

2. npm 镜像源之前是淘宝源，缺少 @quartz-community/types 包，
   已改为官方源：npm config set registry https://registry.npmjs.org/

3. 如果 CF 上 install-plugins 还是失败，可能需要先把插件的 node_modules 和 dist
   一起提交到 git。可以尝试本地运行：
   npm run install-plugins
   然后把 .quartz/plugins/ 整个提交上去。

4. 本地的 git 已经清理过了，release-ver 分支只有 3 个提交，大小约 15MB。
   所有的内容和工作都在。如果还有文件丢失，可以从 commit 49b524a 或 1dd872a 恢复。

5. 当前本地 release-ver 分支与远程 release-ver 已同步。

6. 详细交接文档在项目根目录的 交接文档.md 中。
```
